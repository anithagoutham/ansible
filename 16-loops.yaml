- name: loops demo
  hosts: web
  # connection: local
  tasks: 
  - name: print names
    ansible.builtin.debug:
      msg: "hello {{ items }}"
    loop:
    - goutham
    - anitha
    - suresh
    - chocolate
